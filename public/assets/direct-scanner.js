document.addEventListener('DOMContentLoaded',()=>{
  const original=document.querySelector('#start-scan');if(!original)return;
  const start=original.cloneNode(true);original.replaceWith(start);
  const status=document.querySelector('#scan-status'),video=document.querySelector('#scanner-video'),csrf=document.querySelector('#csrf-token')?.dataset.token;let stream;
  const valid=value=>{const digits=value.replace(/\D/g,'');if(![8,12,13,14].includes(digits.length))return false;let sum=0;for(let i=0;i<digits.length-1;i++)sum+=Number(digits[i])*(((digits.length-2-i)%2===0)?3:1);return (10-(sum%10))%10===Number(digits[digits.length-1]);};
  const stop=()=>{stream?.getTracks().forEach(track=>track.stop());stream=null;video.srcObject=null;video.hidden=true;start.textContent='Open achtercamera';};
  const useBarcode=(value,photo='')=>{stop();status.innerHTML='Cijfers <strong>'+value+'</strong> gelezen.';if(new URLSearchParams(location.search).get('return')==='add'){sessionStorage.setItem('barcode-for-add',JSON.stringify({barcode:value,photo}));location.href='?page=add';return;}sessionStorage.setItem('barcode-scan-photo',photo);checkOwned(value);};
  const readWithVision=async()=>{if(!video.videoWidth)return;status.textContent='Barcodefoto maken en cijfers lezen…';const canvas=document.createElement('canvas'),top=Math.floor(video.videoHeight*.28);canvas.width=video.videoWidth;canvas.height=video.videoHeight-top;canvas.getContext('2d').drawImage(video,0,top,video.videoWidth,canvas.height,0,0,canvas.width,canvas.height);
    if('BarcodeDetector' in window){try{const detector=new BarcodeDetector({formats:['ean_13','ean_8','upc_a','upc_e']});const result=await detector.detect(canvas);const value=(result[0]?.rawValue||'').replace(/\D/g,'');if(valid(value)){useBarcode(value);return;}}catch(error){}}
    const image=await new Promise(resolve=>canvas.toBlob(resolve,'image/jpeg',.98));if(!image){status.textContent='Barcodefoto kon niet worden gemaakt.';return;}const form=new FormData();form.append('barcode_photo',image,'barcode.jpg');form.append('csrf',csrf||'');try{const response=await fetch('?action=vision-barcode',{method:'POST',body:form});const data=await response.json();if(!response.ok||!data.barcode)throw new Error(data.error||'Onbekende fout');useBarcode(data.barcode,data.scan_photo||'');}catch(error){status.textContent=error.message||'Cijfers konden niet worden gelezen. Probeer een scherpere foto.';}};
  start.addEventListener('click',async()=>{if(stream){await readWithVision();return;}try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{exact:'environment'},width:{ideal:1920},height:{ideal:1080}},audio:false});video.srcObject=stream;video.hidden=false;await video.play();start.textContent='Lees cijfers uit foto';status.textContent='Richt de volledige barcode inclusief de cijfers links en rechts in beeld en tik op “Lees cijfers uit foto”.';}catch(error){status.textContent='De achtercamera kon niet worden geopend. Controleer cameratoestemming en HTTPS.';}});
});
